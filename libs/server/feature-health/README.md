# feature-health

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-health` to build the library.

## Running unit tests

Run `nx test feature-health` to execute the unit tests via [Vitest](https://vitest.dev/).
